// while loop
int i = 1;
while(i <= 5) {
  println(i);
  i++;
}

// for loop
for (i = 1; i <= 5; i++) {
  println(i);
}

// many ways to increment by 1:
i = i + 1;
// if using an array, do not need to solve a complicated equation twice to access indexes
i += 1;

// computer has very fast incrementation and decrementation function
i++; // do the addition last in something like x = i++; 
++i; // do the addition first in something like x = ++i;

// inclusive of first num, exclusive of second num
int(random(4,5));
